import React from 'react';
import { View } from 'react-native';
import styled from 'styled-components/native';

export default LoginContainer = styled.View`
  position: relative;
  height: 40%;
  display: flex;
  align-items: center;
`;

